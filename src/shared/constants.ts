import {theme} from '../assets/theme';

export const POSTER_ASPECT_RATIO = 480 / 714;

/**
 * Initial left space applied only for section title, first section item
 * creates spacing illusion, allows scrolling items all the way left
 */
export const HOME_SCREEN_LEFT_SPACING = theme.space.m;
